'use strict';

const START = 'start';

const JWT = 'jwt';

const PROTECT = 'protect';

const INPUT = 'input';

const EXEC = 'exec';

const POST = 'post';

module.exports = {

    START,
    JWT,
    PROTECT,
    INPUT,
    EXEC,
    POST
};
