'use strict';

const START = 'start';

const JWT = 'jwt';

const INPUT = 'input';

const EXEC = 'exec';

const POST = 'post';

module.exports = {

    START,
    JWT,
    INPUT,
    EXEC,
    POST
};
