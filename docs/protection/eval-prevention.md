# Preventing calls to `eval()`

The default configuration of Vandium will prevent calls to `eval()` and will cause an exception to be thrown. To disable this functionality:
set the `VANDIUM_PREVENT_EVAL` to `"false"`.
